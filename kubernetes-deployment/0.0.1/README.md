<!-- BEGIN_TF_DOCS -->
## Requirements

No requirements.

## Providers

| Name | Version |
|------|---------|
| <a name="provider_kubernetes"></a> [kubernetes](#provider\_kubernetes) | 2.20.0 |

## Modules

No modules.

## Resources

| Name | Type |
|------|------|
| [kubernetes_deployment.deploy_app](https://registry.terraform.io/providers/hashicorp/kubernetes/latest/docs/resources/deployment) | resource |

## Inputs

| Name | Description | Type | Default | Required |
|------|-------------|------|---------|:--------:|
| <a name="input_add_capabilities"></a> [add\_capabilities](#input\_add\_capabilities) | A list of added capabilities | `list(string)` | `null` | no |
| <a name="input_args"></a> [args](#input\_args) | Arguments to the entrypoint | `list(string)` | `[]` | no |
| <a name="input_auto_mounted_service_account_token"></a> [auto\_mounted\_service\_account\_token](#input\_auto\_mounted\_service\_account\_token) | Indicates whether a service account token should be automatically mounted | `bool` | `null` | no |
| <a name="input_command"></a> [command](#input\_command) | Entrypoint array. Not executed within a shell | `list(string)` | `[]` | no |
| <a name="input_container_name"></a> [container\_name](#input\_container\_name) | Name of the container | `string` | n/a | yes |
| <a name="input_deployment_annotations"></a> [deployment\_annotations](#input\_deployment\_annotations) | Annotations for deployment | `map(string)` | `null` | no |
| <a name="input_deployment_labels"></a> [deployment\_labels](#input\_deployment\_labels) | Labels for deployment | `map(string)` | `null` | no |
| <a name="input_dns_policy"></a> [dns\_policy](#input\_dns\_policy) | DNS policy for containers within the pod | `string` | `"ClusterFirst"` | no |
| <a name="input_drop_capabilities"></a> [drop\_capabilities](#input\_drop\_capabilities) | A list of removed capabilities | `list(string)` | `null` | no |
| <a name="input_env"></a> [env](#input\_env) | Name and value pairs to set in the container's environment | `map(string)` | `{}` | no |
| <a name="input_fs_group"></a> [fs\_group](#input\_fs\_group) | A special supplemental group that applies to all containers in a pod | `number` | `null` | no |
| <a name="input_host_aliases"></a> [host\_aliases](#input\_host\_aliases) | List of hosts and IPs that will be injected into the pod's hosts file if specified | <pre>list(object({<br>    hostnames = list(string)<br>    ip        = string<br>  }))</pre> | `[]` | no |
| <a name="input_hostname"></a> [hostname](#input\_hostname) | Specifies the hostname of the Pod If not specified, the pod's hostname will be set to a system-defined value | `string` | `null` | no |
| <a name="input_image"></a> [image](#input\_image) | Docker image name | `string` | n/a | yes |
| <a name="input_image_pull_policy"></a> [image\_pull\_policy](#input\_image\_pull\_policy) | Always. Always pull the image. ; IfNotPresent. Only pull the image if it does not already exist on the node. ; Never. Never pull the image. | `string` | `"IfNotPresent"` | no |
| <a name="input_image_pull_secrets"></a> [image\_pull\_secrets](#input\_image\_pull\_secrets) | Specify list of pull secrets | `list(string)` | `[]` | no |
| <a name="input_lifecycle_events"></a> [lifecycle\_events](#input\_lifecycle\_events) | Actions that the management system should take in response to container lifecycle events, include postStart and preStop | <pre>list(object({<br>    pre_stop = optional(object({<br>      initial_delay_seconds = optional(string)<br>      period_seconds        = optional(string)<br>      timeout_seconds       = optional(string)<br>      success_threshold     = optional(string)<br>      failure_threshold     = optional(string)<br>      http_get = optional(object({<br>        path   = string<br>        port   = string<br>        scheme = string<br>        host   = string<br>        http_header = list(object({<br>          name  = string<br>          value = string<br>        }))<br>      }))<br>      exec = optional(object({<br>        command = list(string)<br>      }))<br>      tcp_socket = optional(object({<br>        port = number<br>      }))<br>    }))<br>    post_start = optional(object({<br>      initial_delay_seconds = optional(string)<br>      period_seconds        = optional(string)<br>      timeout_seconds       = optional(string)<br>      success_threshold     = optional(string)<br>      failure_threshold     = optional(string)<br>      http_get = optional(object({<br>        path   = string<br>        port   = string<br>        scheme = string<br>        host   = string<br>        http_header = list(object({<br>          name  = string<br>          value = string<br>        }))<br>      }))<br>      exec = optional(object({<br>        command = list(string)<br>      }))<br>      tcp_socket = optional(object({<br>        port = number<br>      }))<br>    }))<br>  }))</pre> | `[]` | no |
| <a name="input_limit_cpu"></a> [limit\_cpu](#input\_limit\_cpu) | CPU limit. e.g. 0.5, 1, 2 | `string` | `""` | no |
| <a name="input_limit_memory"></a> [limit\_memory](#input\_limit\_memory) | Memory limit. e.g. 128Mi, 512Mi, 1Gi, 2Gi, 4Gi | `string` | `""` | no |
| <a name="input_liveness_probe"></a> [liveness\_probe](#input\_liveness\_probe) | Container will be restarted if the probe fails. | <pre>list(object({<br>    initial_delay_seconds = optional(string)<br>    period_seconds        = optional(string)<br>    timeout_seconds       = optional(string)<br>    success_threshold     = optional(string)<br>    failure_threshold     = optional(string)<br>    http_get = optional(object({<br>      path   = string<br>      port   = string<br>      scheme = string<br>      host   = string<br>      http_header = optional(list(object({<br>        name  = string<br>        value = string<br>      })))<br>    }))<br>    exec = optional(object({<br>      command = list(string)<br>    }))<br>    tcp_socket = optional(object({<br>      port = number<br>    }))<br>  }))</pre> | `[]` | no |
| <a name="input_max_surge"></a> [max\_surge](#input\_max\_surge) | The maximum number of pods that can be scheduled above the desired number of pods | `string` | `"25%"` | no |
| <a name="input_max_unavailable"></a> [max\_unavailable](#input\_max\_unavailable) | The maximum number of pods that can be unavailable during the update | `string` | `"25%"` | no |
| <a name="input_min_ready_seconds"></a> [min\_ready\_seconds](#input\_min\_ready\_seconds) | Minimum number of seconds for which a newly created pod should be ready without any of its container crashing, for it to be considered available | `number` | `0` | no |
| <a name="input_name"></a> [name](#input\_name) | Name of the deployment | `string` | n/a | yes |
| <a name="input_nameservers"></a> [nameservers](#input\_nameservers) | A list of DNS name server IP addresses specified as strings | `list(string)` | `null` | no |
| <a name="input_namespace"></a> [namespace](#input\_namespace) | Namespace in which to create the deployment | `string` | `"default"` | no |
| <a name="input_networking_mode"></a> [networking\_mode](#input\_networking\_mode) | Whether host networking requested for this pod | `string` | `"Normal"` | no |
| <a name="input_node_affinity"></a> [node\_affinity](#input\_node\_affinity) | @label "Node Selector" @group "Pod/Scheduling" @show\_if "node\_scheduling='Run pods on node(s) marching scheduling rules'" | <pre>list(object(<br>    {<br>      preferred_during_scheduling_ignored_during_execution = optional(list(object(<br>        {<br>          preference = list(object(<br>            {<br>              match_expressions = list(object(<br>                {<br>                  key      = string<br>                  operator = string<br>                  values   = set(string)<br>                }<br>              ))<br>            }<br>          ))<br>          weight = number<br>        }<br>      )))<br>      required_during_scheduling_ignored_during_execution = optional(list(object(<br>        {<br>          node_selector_term = list(object(<br>            {<br>              match_expressions = list(object(<br>                {<br>                  key      = string<br>                  operator = string<br>                  values   = set(string)<br>                }<br>              ))<br>            }<br>          ))<br>        }<br>      )))<br>    }<br>  ))</pre> | `[]` | no |
| <a name="input_node_name"></a> [node\_name](#input\_node\_name) | NodeName is a request to schedule this pod onto a specific node | `string` | `null` | no |
| <a name="input_node_scheduling"></a> [node\_scheduling](#input\_node\_scheduling) | Node scheduling | `string` | `"Run pods on any available node"` | no |
| <a name="input_pod_affinity"></a> [pod\_affinity](#input\_pod\_affinity) | @label "Pod Affinity" @group "Pod/Pod Scheduling" | <pre>list(object(<br>    {<br>      preferred_during_scheduling_ignored_during_execution = optional(list(object(<br>        {<br>          pod_affinity_term = list(object(<br>            {<br>              label_selector = list(object(<br>                {<br>                  match_expressions = list(object(<br>                    {<br>                      key      = string<br>                      operator = string<br>                      values   = set(string)<br>                    }<br>                  ))<br>                  match_labels = map(string)<br>                }<br>              ))<br>              namespaces   = set(string)<br>              topology_key = string<br>            }<br>          ))<br>          weight = number<br>        }<br>      )))<br>      required_during_scheduling_ignored_during_execution = optional(list(object(<br>        {<br>          label_selector = object(<br>            {<br>              match_expressions = list(object(<br>                {<br>                  key      = string<br>                  operator = string<br>                  values   = set(string)<br>                }<br>              ))<br>              match_labels = map(string)<br>            }<br>          )<br>          namespaces   = set(string)<br>          topology_key = string<br>        }<br>      )))<br>    }<br>  ))</pre> | `[]` | no |
| <a name="input_pod_anti_affinity"></a> [pod\_anti\_affinity](#input\_pod\_anti\_affinity) | @label "Pod Anti Affinity" @group "Pod/Pod Scheduling" | <pre>list(object(<br>    {<br>      preferred_during_scheduling_ignored_during_execution = optional(list(object(<br>        {<br>          pod_affinity_term = list(object(<br>            {<br>              label_selector = list(object(<br>                {<br>                  match_expressions = list(object(<br>                    {<br>                      key      = string<br>                      operator = string<br>                      values   = set(string)<br>                    }<br>                  ))<br>                  match_labels = map(string)<br>                }<br>              ))<br>              namespaces   = set(string)<br>              topology_key = string<br>            }<br>          ))<br>          weight = number<br>        }<br>      )))<br>      required_during_scheduling_ignored_during_execution = optional(list(object(<br>        {<br>          label_selector = object(<br>            {<br>              match_expressions = list(object(<br>                {<br>                  key      = string<br>                  operator = string<br>                  values   = set(string)<br>                }<br>              ))<br>              match_labels = map(string)<br>            }<br>          )<br>          namespaces   = set(string)<br>          topology_key = string<br>        }<br>      )))<br>    }<br>  ))</pre> | `[]` | no |
| <a name="input_ports"></a> [ports](#input\_ports) | List of ports to expose from the container | <pre>list(object({<br>    name          = string<br>    internal_port = number<br>    host_port     = optional(string)<br>  }))</pre> | `[]` | no |
| <a name="input_priority_class_name"></a> [priority\_class\_name](#input\_priority\_class\_name) | If specified, indicates the pod's priority | `string` | `null` | no |
| <a name="input_privileged"></a> [privileged](#input\_privileged) | Whether this container has full access to the host | `bool` | `false` | no |
| <a name="input_privileged_escalation"></a> [privileged\_escalation](#input\_privileged\_escalation) | Whether this container can gain more privileges than its parent process | `bool` | `true` | no |
| <a name="input_progress_deadline"></a> [progress\_deadline](#input\_progress\_deadline) | The maximum time in seconds for a deployment to make progress before it is considered to be failed | `number` | `600` | no |
| <a name="input_read_only_root_filesystem"></a> [read\_only\_root\_filesystem](#input\_read\_only\_root\_filesystem) | Whether this container has a read-only root filesystem | `bool` | `false` | no |
| <a name="input_readiness_probe"></a> [readiness\_probe](#input\_readiness\_probe) | Container will be removed from service endpoints if the probe fails. | <pre>list(object({<br>    initial_delay_seconds = string<br>    period_seconds        = string<br>    timeout_seconds       = string<br>    success_threshold     = string<br>    failure_threshold     = string<br>    http_get = object({<br>      path   = string<br>      port   = string<br>      scheme = string<br>      host   = string<br>      http_header = list(object({<br>        name  = string<br>        value = string<br>      }))<br>    })<br>    exec = object({<br>      command = list(string)<br>    })<br>    tcp_socket = object({<br>      port = number<br>    })<br>  }))</pre> | `[]` | no |
| <a name="input_replicas"></a> [replicas](#input\_replicas) | Count of pods | `number` | `1` | no |
| <a name="input_request_cpu"></a> [request\_cpu](#input\_request\_cpu) | CPU request. e.g. 0.5, 1, 2 | `string` | `"0.1"` | no |
| <a name="input_request_memory"></a> [request\_memory](#input\_request\_memory) | Memory request. e.g. 128Mi, 512Mi, 1Gi, 2Gi, 4Gi | `string` | `"128Mi"` | no |
| <a name="input_resolver_options"></a> [resolver\_options](#input\_resolver\_options) | A list of DNS resolver options specified as blocks with name/value pairs | `list(map(string))` | `[]` | no |
| <a name="input_restart_policy"></a> [restart\_policy](#input\_restart\_policy) | Restart policy for all containers within the pod. One of Always, OnFailure, Never | `string` | `"Always"` | no |
| <a name="input_revision_history_limit"></a> [revision\_history\_limit](#input\_revision\_history\_limit) | The number of old ReplicaSets to retain to allow rollback | `number` | `10` | no |
| <a name="input_run_as_non_root"></a> [run\_as\_non\_root](#input\_run\_as\_non\_root) | Whether this container must run as a non-root user | `bool` | `false` | no |
| <a name="input_run_as_user"></a> [run\_as\_user](#input\_run\_as\_user) | The UID to run the entrypoint of the container process | `string` | `null` | no |
| <a name="input_search_domains"></a> [search\_domains](#input\_search\_domains) | A list of DNS search domains for host-name lookup specified as strings | `list(string)` | `null` | no |
| <a name="input_service_account_name"></a> [service\_account\_name](#input\_service\_account\_name) | Is the name of the ServiceAccount to use to run this pod | `string` | `null` | no |
| <a name="input_startup_probe"></a> [startup\_probe](#input\_startup\_probe) | Container will wait util this check succeeds before attempting other health checks. | <pre>list(object({<br>    initial_delay_seconds = string<br>    period_seconds        = string<br>    timeout_seconds       = string<br>    success_threshold     = string<br>    failure_threshold     = string<br>    http_get = object({<br>      path   = string<br>      port   = string<br>      scheme = string<br>      host   = string<br>      http_header = list(object({<br>        name  = string<br>        value = string<br>      }))<br>    })<br>    exec = object({<br>      command = list(string)<br>    })<br>    tcp_socket = object({<br>      port = number<br>    })<br>  }))</pre> | `[]` | no |
| <a name="input_stdin"></a> [stdin](#input\_stdin) | Stdin for container | `string` | `"No"` | no |
| <a name="input_strategy"></a> [strategy](#input\_strategy) | Strategy of deployment update | `string` | `"RollingUpdate"` | no |
| <a name="input_subdomain"></a> [subdomain](#input\_subdomain) | If specified, the fully qualified Pod hostname will be '…svc.' | `string` | `null` | no |
| <a name="input_template_annotations"></a> [template\_annotations](#input\_template\_annotations) | Annotations for pod (template) | `map(string)` | `null` | no |
| <a name="input_template_labels"></a> [template\_labels](#input\_template\_labels) | Labels for pod (template) | `map(string)` | `null` | no |
| <a name="input_termination_grace_period_seconds"></a> [termination\_grace\_period\_seconds](#input\_termination\_grace\_period\_seconds) | Duration in seconds the pod needs to terminate gracefully | `number` | `null` | no |
| <a name="input_toleration"></a> [toleration](#input\_toleration) | Pod node tolerations | <pre>list(object({<br>    effect             = optional(string)<br>    key                = optional(string)<br>    operator           = optional(string)<br>    toleration_seconds = optional(string)<br>    value              = optional(string)<br>  }))</pre> | `[]` | no |
| <a name="input_tty"></a> [tty](#input\_tty) | Whether this container should allocate a TTY for itself | `bool` | `true` | no |
| <a name="input_volume_claim"></a> [volume\_claim](#input\_volume\_claim) | Represents an Persistent volume Claim resource that is attached to a kubelet's host machine and then exposed to the pod | <pre>list(object({<br>    volume_name = string<br>    claim_name  = optional(string)<br>    read_only   = optional(string)<br>  }))</pre> | `[]` | no |
| <a name="input_volume_config_map"></a> [volume\_config\_map](#input\_volume\_config\_map) | The data stored in a ConfigMap object can be referenced in a volume of type configMap and then consumed by containerized applications running in a Pod | <pre>list(object({<br>    volume_name = string<br>    name        = string<br>    mode        = optional(string)<br>    optional    = optional(string)<br>    items = optional(list(object({<br>      key  = string<br>      path = string<br>      mode = optional(string)<br>    })), [])<br>  }))</pre> | `[]` | no |
| <a name="input_volume_empty_dir"></a> [volume\_empty\_dir](#input\_volume\_empty\_dir) | EmptyDir represents a temporary directory that shares a pod's lifetime | <pre>list(object({<br>    volume_name = string<br>    medium      = optional(string)<br>    size_limit  = optional(string)<br>  }))</pre> | `[]` | no |
| <a name="input_volume_mount"></a> [volume\_mount](#input\_volume\_mount) | Mount path from pods to volume | <pre>list(object({<br>    volume_name = string<br>    mount_path  = string<br>    sub_path    = optional(string)<br>    read_only   = optional(string)<br>  }))</pre> | `[]` | no |
| <a name="input_volume_secret"></a> [volume\_secret](#input\_volume\_secret) | Create volume from secret | <pre>list(object({<br>    volume_name  = string<br>    secret_name  = string<br>    default_mode = optional(string)<br>    optional     = optional(string)<br>    items = optional(list(object({<br>      key  = string<br>      path = string<br>      mode = optional(string)<br>    })), [])<br>  }))</pre> | `[]` | no |
| <a name="input_wait_for_rollout"></a> [wait\_for\_rollout](#input\_wait\_for\_rollout) | Wait for the deployment to successfully roll out. | `bool` | `true` | no |
| <a name="input_working_dir"></a> [working\_dir](#input\_working\_dir) | Container's working directory. If not specified, the container runtime's default will be used, which might be configured in the container image. | `string` | `null` | no |

## Outputs

| Name | Description |
|------|-------------|
| <a name="output_name"></a> [name](#output\_name) | n/a |
| <a name="output_namespace"></a> [namespace](#output\_namespace) | n/a |
<!-- END_TF_DOCS -->